<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.codembeded.productratecalculator">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".activities.UpdateBagActivity"
            android:label="Update Bag"></activity>
        <activity
            android:name=".activities.UpdateProductActivity"
            android:label="Update Product" />
        <activity
            android:name=".activities.ProductListActivity"
            android:label="Product List" />
        <activity
            android:name=".activities.BagListActivity"
            android:label="Bag List" />
        <activity
            android:name=".activities.CalculatorActivity"
            android:label="Calculator" />
        <activity
            android:name=".activities.BagActivity"
            android:label="Bag" />
        <activity
            android:name=".activities.ProductActivity"
            android:label="Product" />
        <activity android:name=".activities.HomeActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>